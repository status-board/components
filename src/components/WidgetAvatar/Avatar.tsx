import styled from '@emotion/styled';

const Avatar = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    overflow: hidden;
`;

export default Avatar;
